WORKFLOW
-Sử dụng label (for='name_id') cho thẻ input(type="checkbox" id='name_id') khi click vào button sử dụng thẻ label, thì trỏ tới checkbox
sau đó dùng lớp giả .class1:checked ~ .class2 để hiển thị thứ user cần khi tương tác với giao diện
